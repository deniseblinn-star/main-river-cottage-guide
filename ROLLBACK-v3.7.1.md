# v3.7.1 Rollback
If v3.7.1 causes a regression, revert the v3.7.1 Git commit in GitKraken and push the revert. This release contains no required data migration, so rollback does not require changing browser localStorage.
