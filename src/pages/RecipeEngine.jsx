import { useState } from 'react'
import { getRecipeEngineData,getScaledRecipe } from '../utils/recipeEngine'
export default function RecipeEngine(){const data=getRecipeEngineData();const[open,setOpen]=useState(data.mealAssignments[0].id);return <div className="space-y-5"><div><h1 className="page-title">Recipe Engine</h1><p className="text-stone">Attendance scales recipes. Recipes generate groceries.</p></div><div className="bg-forest/5 border border-forest/15 rounded-2xl p-4"><b>Working first slice</b><p className="text-sm text-stone mt-1">Sous Vide Filet, Lonita's Caesar Dressing and Fresh Guacamole.</p></div>{data.mealAssignments.map(a=>{const x=getScaledRecipe(a),expanded=open===a.id;return <section className="card" key={a.id}><button className="w-full text-left" onClick={()=>setOpen(expanded?null:a.id)}><div className="flex justify-between gap-3"><div><p className="section-title">{a.mealName}</p><h2 className="text-xl font-extrabold text-navy">{x.recipe.name}</h2><p className="text-sm text-stone">{a.attendance} attending · yield {x.recipe.yield.quantity} servings</p></div><span className="badge-forest">×{x.scale.toFixed(2)}</span></div></button>{expanded&&<div className="mt-5">{x.recipe.tradition&&<div className="bg-amber-50 border border-amber-200 rounded-2xl p-4 mb-4"><b>Tradition</b><p className="text-sm mt-1">{x.recipe.tradition}</p></div>}<div className="overflow-x-auto rounded-2xl border bg-white"><table className="w-full text-sm"><thead className="bg-cream text-left"><tr><th className="p-3">Ingredient</th><th className="p-3">Original</th><th className="p-3">For {a.attendance}</th></tr></thead><tbody>{x.ingredients.map(i=><tr className="border-t" key={i.ingredientId}><td className="p-3 font-semibold">{i.ingredient.name}</td><td className="p-3">{i.quantity} {i.unit}</td><td className="p-3 font-extrabold text-forest">{i.scaledQuantity} {i.unit}</td></tr>)}</tbody></table></div></div>}</section>})}</div>}
